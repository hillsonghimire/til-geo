## Useful UFW commands


```bash
# check ufw status
sudo ufw enable

# enable ufw
sudo ufw status

# allow or deny the port/protocol
sudo ufw allow <port>/<protocol>
sudo ufw deny <port>/<protocol>


# reset ufw to default setting and disable it
sudo ufw reset

# to disable ufw
sudo ufw disable

```